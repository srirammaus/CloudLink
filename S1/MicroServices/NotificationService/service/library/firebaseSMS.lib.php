<?php
/**
 * Actaully this is not real sms sender instead it sends any SMS through firebase cloud and there need to be 
 * respected application installed users phone and that app checks the phone number of that device and we authenticate via this app
 * 
 * 1.users insisted to download the app before registering
 * 2.once the app opened the phone number will got by app .
 * 3. after regiestration backend tries to send otp to that app (which number he type should be associated with it
 * )
 */
?>